/**
 * Media Processing functions
 */

async function addExif(buffer, packname, author) {
  return buffer;
}

async function bass(buffer, options) {
  return buffer;
}

async function circle(buffer) {
  return buffer;
}

async function blur(buffer, options) {
  return buffer;
}

async function attp(text) {
  return Buffer.from([]);
}

async function sticker(buffer, options) {
  return buffer;
}

async function rotate(buffer, angle) {
  return buffer;
}

async function avMix(audioBuffer, videoBuffer) {
  return videoBuffer;
}

async function webp2mp4(buffer) {
  return buffer;
}

async function addID3(buffer, tags) {
  return buffer;
}

async function trim(buffer, start, end) {
  return buffer;
}

module.exports = {
  addExif,
  bass,
  circle,
  blur,
  attp,
  sticker,
  rotate,
  avMix,
  webp2mp4,
  addID3,
  trim
};